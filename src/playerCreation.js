import { Player } from "./Player"; 

// Sign-in stage
// Other code to have a Player create a username
export const player1 = Player('Hungry person');
export const computer = Player('Computer'); // Keep this static as the computer's name
// since it's used in logic... for now.