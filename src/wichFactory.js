export const makeWich = (description, length, anchorArr, axis) => {
    
  return {
    description: description,
    length: length,
    anchorArr: anchorArr,
    axis: axis,

    hit () {

    },
    
    isSunk() {

    }
  }
}